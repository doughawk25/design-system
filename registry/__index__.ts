/**
 * Style registry index. Used by lib/registry and lib/llm.
 * Keys are style names (e.g. "new-york-v4"); values are component name -> item maps.
 */
export const Index: Record<string, Record<string, unknown>> = {
  "new-york-v4": {},
}
